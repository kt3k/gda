diff --git a/node_modules/vocs/_lib/app/index.server.js b/node_modules/vocs/_lib/app/index.server.js
index f74ee70..ecea1d0 100644
--- a/node_modules/vocs/_lib/app/index.server.js
+++ b/node_modules/vocs/_lib/app/index.server.js
@@ -20,7 +20,7 @@ export async function prerender(location) {
     }))).filter(Boolean);
     const { config } = await resolveVocsConfig();
     const { basePath } = config;
-    return (_jsx(ConfigProvider, { config: config, children: _jsx(NuqsAdapter, { children: _jsx(StaticRouter, { location: location, basename: basePath, children: _jsx(Routes, { children: unwrappedRoutes.map((route) => (_jsx(Route, { path: route.path, element: route.element }, route.path))) }) }) }) }));
+    return (_jsx(ConfigProvider, { config: config, children: _jsx(NuqsAdapter, { children: _jsx(StaticRouter, { location: `${basePath ?? ''}${location}`, basename: basePath, children: _jsx(Routes, { children: unwrappedRoutes.map((route) => (_jsx(Route, { path: route.path, element: route.element }, route.path))) }) }) }) }));
 }
 export async function render(req) {
     const { config } = await resolveVocsConfig();
diff --git a/node_modules/vocs/_lib/vite/plugins/llms.js b/node_modules/vocs/_lib/vite/plugins/llms.js
index 4cdb8da..38964cc 100644
--- a/node_modules/vocs/_lib/vite/plugins/llms.js
+++ b/node_modules/vocs/_lib/vite/plugins/llms.js
@@ -3,6 +3,7 @@ import { dirname, resolve } from 'node:path';
 import { default as fs } from 'fs-extra';
 import { directiveToMarkdown } from 'mdast-util-directive';
 import { gfmToMarkdown } from 'mdast-util-gfm';
+import { mathToMarkdown } from 'mdast-util-math';
 import { mdxToMarkdown } from 'mdast-util-mdx';
 import { mdxJsxToMarkdown } from 'mdast-util-mdx-jsx';
 import { toMarkdown } from 'mdast-util-to-markdown';
@@ -14,7 +15,6 @@ import { visit } from 'unist-util-visit';
 import { resolveVocsConfig } from '../utils/resolveVocsConfig.js';
 import { getRemarkPlugins } from './mdx.js';
 import { filterContentVisibility } from './remark/content-visibility.js';
-const remarkPlugins = getRemarkPlugins();
 export async function llms() {
     let viteConfig;
     return [
@@ -29,6 +29,9 @@ export async function llms() {
                     return;
                 const { config } = await resolveVocsConfig();
                 const { basePath, description, llms, rootDir, title = 'Docs' } = config ?? {};
+                // PATCH: pass user markdown config (e.g. remark-math) so pages with math
+                // don't fail MDX parsing during llms.txt generation.
+                const remarkPlugins = getRemarkPlugins({ markdown: config?.markdown });
                 // Check if generateMarkdown is enabled in config
                 const generateMarkdown = llms?.generateMarkdown ?? false;
                 const content = [`# ${title}`, ''];
@@ -67,7 +70,7 @@ export async function llms() {
                                     if (j && i && j <= i)
                                         return;
                                     found = true;
-                                    description = toMarkdown(n, { extensions: [mdxJsxToMarkdown()] }).trim();
+                                    description = toMarkdown(n, { extensions: [mdxJsxToMarkdown(), mathToMarkdown()] }).trim();
                                     return;
                                 });
                             // Link to .md files if generateMarkdown is enabled, otherwise link to HTML pages
@@ -95,6 +98,8 @@ export async function llms() {
                                 gfmToMarkdown(),
                                 mdxJsxToMarkdown(),
                                 mdxToMarkdown(),
+                                // PATCH: serialize math/inlineMath nodes produced by remark-math
+                                mathToMarkdown(),
                             ],
                         });
                         llmsCtxTxtContent.push(processedMarkdown, '');
diff --git a/node_modules/vocs/_lib/vite/utils/search.js b/node_modules/vocs/_lib/vite/utils/search.js
index 711d325..2b63576 100644
--- a/node_modules/vocs/_lib/vite/utils/search.js
+++ b/node_modules/vocs/_lib/vite/utils/search.js
@@ -15,11 +15,18 @@ import { getRehypePlugins, getRemarkPlugins } from '../plugins/mdx.js';
 import * as cache_ from './cache.js';
 import { hash } from './hash.js';
 import { slash } from './slash.js';
-const limit = pLimit(30);
+import { resolveVocsConfig } from './resolveVocsConfig.js';
+// PATCH: lower concurrency (30 -> 2) — KaTeX-heavy pages are large; indexing 30 at
+// once alongside the Rollup build spikes memory on constrained machines.
+const limit = pLimit(2);
 export const debug = debug_('vocs:search');
 export async function buildIndex({ baseDir, cacheDir }) {
     const cache = cache_.search({ cacheDir });
     const pagesPaths = await Array.fromAsync(glob(`${resolve(baseDir, 'pages')}/**/*.{md,mdx}`));
+    // PATCH: pass user markdown config (e.g. remark-math) so pages with math
+    // don't fail MDX compilation during search indexing.
+    const { config } = await resolveVocsConfig();
+    const remarkPlugins = getRemarkPlugins({ markdown: config.markdown });
     const rehypePlugins = getRehypePlugins({ cacheDir, rootDir: baseDir, twoslash: false });
     const documents = await Promise.all(pagesPaths.map((pagePath) => limit(async (pagePath) => {
         const mdx = readFileSync(pagePath, 'utf-8');
@@ -29,8 +36,10 @@ export async function buildIndex({ baseDir, cacheDir }) {
             return pageCache.document;
         // Temporary fix to include files with components in the search index until the todo in `processMdx()` is resolved
         const mdxNoImports = mdx.replace(/^(import|export).*/gm, '');
-        const mdxNoComponents = mdxNoImports.replace(/<\/?\s*[A-Z][^>]*>/g, '');
-        const { html, frontmatter } = await processMdx(pagePath, mdxNoComponents, { rehypePlugins });
+        // PATCH: don't strip `<Capital ...>` sequences — the regex spans newlines and
+        // mangles math like `$a < \Xi$ ... $x > y$`, breaking MDX for math-heavy pages.
+        const mdxNoComponents = mdxNoImports;
+        const { html, frontmatter } = await processMdx(pagePath, mdxNoComponents, { rehypePlugins, remarkPlugins });
         if (frontmatter.searchable === false) {
             cache.set(key, { mdx, document: [] });
             return [];
@@ -77,9 +86,9 @@ export function saveIndex(outDir, index, { cacheDir } = {}) {
     fs.writeJSONSync(join(dir, `search-index-${hash_}.json`), json);
     return hash_;
 }
-const remarkPlugins = getRemarkPlugins();
 export async function processMdx(filePath, file, options) {
-    const { rehypePlugins } = options;
+    // PATCH: use caller-provided remarkPlugins (with user markdown config) when given.
+    const { rehypePlugins, remarkPlugins = getRemarkPlugins() } = options;
     try {
         const compiled = await compile(file, {
             baseUrl: pathToFileURL(filePath).href,
